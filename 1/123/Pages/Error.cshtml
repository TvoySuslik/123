@page
@model ErrorModel
@{
    ViewData["Title"] = "Error"; // Установка заголовка страницы на "Error"
}

<h1 class="text-danger">Error.</h1> <!-- Основной заголовок с красным цветом -->
<h2 class="text-danger">An error occurred while processing your request.</h2> <!-- Подзаголовок с красным цветом -->

@if (Model.ShowRequestId)
{
    <p>
		<strong>Request ID:</strong> <code>@Model.RequestId</code> <!-- Отображение идентификатора запроса, если он доступен -->
    </p>
}

<h3>Development Mode</h3> <!-- Заголовок секции о режиме разработки -->
<p>
    Swapping to the <strong>Development</strong> environment displays detailed information about the error that occurred.
</p> <!-- Информация о режиме разработки, где показываются подробности ошибок -->

<p>
    <strong>The Development environment shouldn't be enabled for deployed applications.</strong>
    It can result in displaying sensitive information from exceptions to end users.
    For local debugging, enable the <strong>Development</strong> environment by setting the <strong>ASPNETCORE_ENVIRONMENT</strong> environment variable to <strong>Development</strong>
    and restarting the app.
</p> <!-- Предупреждение о необходимости отключения режима разработки в опубликованных приложениях -->
